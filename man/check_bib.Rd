% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check.R
\name{check_bib}
\alias{check_bib}
\title{Check a BibTeX or BibLaTeX bibliography for retracted references}
\usage{
check_bib(
  path,
  sources = getOption("retraction.sources", "xera"),
  offline = FALSE,
  flag_nature = c("Retraction", "Expression of Concern"),
  allow_fuzzy = TRUE,
  resolve_ids = TRUE,
  progress = TRUE,
  strict = FALSE
)
}
\arguments{
\item{path}{Path to a \code{.bib} file.}

\item{sources}{Sources to query. A character vector of names from
\code{\link[=list_backends]{list_backends()}}, or \code{"all"}. Defaults to
\code{getOption("retraction.sources", "xera")}.}

\item{offline}{If \code{TRUE}, match against the local snapshot built by
\code{\link[=retraction_sync]{retraction_sync()}} instead of querying the network.}

\item{flag_nature}{Notice labels that count as "flag this citation". Defaults
to Retraction and Expression of Concern.}

\item{allow_fuzzy}{Allow fuzzy title matching for references without a usable
identifier.}

\item{resolve_ids}{If \code{TRUE}, resolve PMID-only references to a DOI via
OpenAlex before matching.}

\item{progress}{Show a progress bar in interactive sessions.}

\item{strict}{If \code{TRUE}, error when any reference cannot be checked (an
\code{unchecked} row from a network or source failure) instead of returning it,
so a failure cannot be mistaken for a clean result. Useful in CI.}
}
\value{
A \code{\link[=print.retraction_result]{retraction_result}} tibble.
}
\description{
A thin wrapper around \code{\link[=check_file]{check_file()}} that forces the BibTeX parser.
}
\examples{
\donttest{
bib <- system.file("extdata", "example.bib", package = "retraction")
if (nzchar(bib)) check_bib(bib)
}
}
