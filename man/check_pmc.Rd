% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pmc.R
\name{check_pmc}
\alias{check_pmc}
\title{Check the reference lists of open-access PubMed Central articles}
\usage{
check_pmc(
  x,
  sources = getOption("retraction.sources", "xera"),
  offline = FALSE,
  flag_nature = c("Retraction", "Expression of Concern"),
  allow_fuzzy = TRUE,
  resolve_ids = TRUE,
  cache = TRUE,
  progress = TRUE
)
}
\arguments{
\item{x}{A character (or numeric) vector of PMIDs, PMCIDs, DOIs, titles, or
reference strings.}

\item{sources}{Sources to query. A character vector of names from
\code{\link[=list_backends]{list_backends()}}, or \code{"all"}. Defaults to
\code{getOption("retraction.sources", "xera")}.}

\item{offline}{If \code{TRUE}, match against the local snapshot built by
\code{\link[=retraction_sync]{retraction_sync()}} instead of querying the network.}

\item{flag_nature}{Notice labels that count as "flag this citation". Defaults
to Retraction and Expression of Concern.}

\item{allow_fuzzy}{Allow fuzzy title matching for references without a usable
identifier.}

\item{resolve_ids}{If \code{TRUE}, resolve PMID-only references to a DOI via
OpenAlex before matching.}

\item{cache}{Cache fetched XML on disk (see \code{\link[=pmc_fetch_xml]{pmc_fetch_xml()}}).}

\item{progress}{Show a progress bar in interactive sessions.}
}
\value{
A \code{\link[=print.retraction_result]{retraction_result}} tibble of the
references found across the open-access articles (the \code{source_file} column
records the PMCID). The per-article open-access summary is attached and
retrievable with \code{\link[=pmc_articles]{pmc_articles()}}.
}
\description{
For each input, resolves it to a PubMed Central article, reports whether the
open-access full text (with a reference list) is available, and if so checks
every reference in that article for retractions. Inputs may be a PMID, PMCID,
DOI, article title, or a whole reference string, in any mix.
}
\details{
Resolving and fetching the article always use the network, even when
\code{offline = TRUE} (which controls only the retraction data source used for
matching). Per-article open-access status is available via \code{\link[=pmc_articles]{pmc_articles()}}.
}
\examples{
\dontrun{
res <- check_pmc(c("PMC5334499", "10.1371/journal.pone.0000217", "29939664"))
pmc_articles(res)   # open-access status per input
retracted(res)      # any retracted references found
}
}
\seealso{
\code{\link[=pmc_articles]{pmc_articles()}}, \code{\link[=pmc_fetch_xml]{pmc_fetch_xml()}}
}
