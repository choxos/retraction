% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporal.R
\name{classify_timing}
\alias{classify_timing}
\title{Classify each citation relative to the document's date.}
\usage{
classify_timing(x, document_date = Sys.Date(), citation_dates = NULL)
}
\arguments{
\item{x}{A \code{retraction_result}.}

\item{document_date}{The document's date, \code{Date} or \code{YYYY-MM-DD}. Defaults to
today; see \code{\link[=manuscript_date_of]{manuscript_date_of()}} to derive it from a file.}

\item{citation_dates}{Optional named vector (\code{Date}/string) of per-citation
dates, named by \code{id} or DOI. When supplied for a row, that date is used and
the label becomes \code{"cited_after_retraction"} / \code{"cited_before_retraction"}.}
}
\value{
\code{x} with an added character \code{timing} column (\code{NA} when no retraction
date is known).
}
\description{
Adds a \code{timing} column. The default date is the document's authoring date
(git commit or mtime), which is \strong{not} the date a specific citation was
added, so the labels are deliberately conservative:
\code{"document_after_retraction"} means the document was written after the work
was retracted, not that the citation was knowingly added post-retraction. Pass
\code{citation_dates} for true citation-level timing.
}
\examples{
\donttest{
res <- check_dois("10.1016/S0140-6736(97)11096-0")
classify_timing(res, document_date = "2015-01-01")
}
}
