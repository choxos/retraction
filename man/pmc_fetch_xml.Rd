% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pmc.R
\name{pmc_fetch_xml}
\alias{pmc_fetch_xml}
\title{Retrieve open-access PMC full-text XML for a PMCID}
\usage{
pmc_fetch_xml(pmcid, cache = TRUE, overwrite = FALSE)
}
\arguments{
\item{pmcid}{A PMCID (any form accepted by \code{\link[=normalize_pmcid]{normalize_pmcid()}}).}

\item{cache}{Cache the XML on disk and reuse it on later calls.}

\item{overwrite}{Re-fetch even if a cache file exists.}
}
\value{
An \code{xml_document}, or \code{NULL} if the article could not be retrieved.
}
\description{
Tries NCBI EFetch (\code{db = pmc}) first, then the PMC OAI-PMH service. Results
are cached on disk under \code{\link[=retraction_cache_dir]{retraction_cache_dir()}}; an existing non-empty cache
file is reused unless \code{overwrite = TRUE}.
}
\examples{
\dontrun{
doc <- pmc_fetch_xml("PMC5334499")
}
}
