% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporal.R
\name{exposure_score}
\alias{exposure_score}
\title{Retraction-exposure summary, with denominator diagnostics.}
\usage{
exposure_score(x)
}
\arguments{
\item{x}{A \code{retraction_result}.}
}
\value{
A named list: \code{n_total}, \code{n_checked}, \code{n_flagged}, \code{n_possible},
\code{n_unchecked}, \code{flagged_per_total}, \code{flagged_per_checked}.
}
\description{
A bare \code{flagged / n} headline is misleading when many rows are unchecked or
only "possible" (future-work review #7), so this reports the full breakdown
and two rates: per total, and per successfully-checked.
}
\examples{
\donttest{
res <- check_dois("10.1016/S0140-6736(97)11096-0")
exposure_score(res)
}
}
